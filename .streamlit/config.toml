[client]
showErrorDetails = false
toolbarMode = "minimal"

# [deployment]
# showDeploymentButton = false

[server]
# Ensures the app only listens to your local machine, not the network
address = "127.0.0.1"

[browser]
gatherUsageStats = false

[theme]
primaryColor = "#003366"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#001F54"

